
server:
  port: 8070

spring:
  datasource:
    url: jdbc:h2:file:./data/EasyMoneyPagamento 
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
   console:
    enabled: true        